
# Null : Null হল খালি মান ইচ্ছাকৃতভাবে মান  খালি রাখা হয় 
# Undefined হলো যখন কোনো ভেরিয়েবল ডিফাইন করা হয়েছে কিন্তু কোনো মান দেওয়া হয়নি

# map() ফাংশন এটি একটি নতুন অ্যারের জন্য ব্যবহৃত হয়। মূল অ্যারে পরিবর্তন না করে নতুন অ্যারে তৈরি করে। forEach() ফাংশনএটি মূল অ্যারের উপর কাজ করে, কিন্তু কোনো নতুন অ্যারে তৈরি করে না। শুধুমাত্র প্রতিটি উপাদানের জন্য ফাংশন চালায়।  

# == এটি মান তুলনা করে এবং ডাটা টাইপে রূপান্তর করে  মানগুলি সমতুল্য কিনা তা পরীক্ষা করে । === এটি মান তুলনা করে  মানগুলি সমতুল্য কিনা তা পরীক্ষা করে । যদি মান  সমান না হয় তাহলে false আউটপুট প্রধান করে 

# Api থেকে ডেটা আনতে  পারি এন্ড Asynchronous কোডকে Synchronous এর মতো লেখা যায় & await দিয়ে আমরা ডেটা আসা পর্যন্ত অপেক্ষা করি।  

# কোনো ভেরিয়েবল ফাংশনের বাইরে ডিক্লেয়ার করলে তা গ্লোবাল স্কোপে থাকে।কোনো 5) ভেরিয়েবল ফাংশনের ভিতরে  ডিক্লেয়ার করলে তা শুধু ফাংশন এর জন্য এক্সেসযোগ্য থাকে | Let ,const ভেরিয়েবল ডিক্লেয়ার করা হয় তা ব্লক স্কোপ


